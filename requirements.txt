# JARVIS Python dependencies
# Install with: pip install -r requirements.txt

# Core audio
sounddevice>=0.4.6
numpy>=1.24.0
scipy>=1.11.0

# Speech-to-text (CUDA build)
faster-whisper>=1.0.0

# WebSocket server (UI bridge)
websockets>=12.0

# HTTP (for Ollama)
# Uses stdlib urllib — no extra dep needed

# Optional: richer terminal output
colorama>=0.4.6
